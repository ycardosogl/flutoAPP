import React from 'react';
import { View, Text, TouchableOpacity, Image, StyleSheety,styles } from 'react-native';
import { useNavigation } from '@react-navigation/native';

const loginProp = () => {








};
export default loginProp;
